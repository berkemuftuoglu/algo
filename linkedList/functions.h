#include <linkedList.h>
#include <imports.h>

class linkedList{
    public:
    ListNode* middleNode(ListNode* head) {
        vector<ListNode> ans{head};
        while(ans->back != NULL) {
            
        }
    }

};